using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class awaikening_review : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Dictionary<string, string> dict4rev = new Dictionary<string, string>(Report_Manager.get_review_on_insList());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
